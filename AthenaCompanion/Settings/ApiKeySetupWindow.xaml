<Window x:Class="AthenaCompanion.Settings.ApiKeySetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OpenAI API Key"
        Width="440"
        Height="230"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="OpenAI API Key"
                   FontSize="18"
                   FontWeight="SemiBold" />

        <TextBlock Grid.Row="1"
                   Margin="0,8,0,14"
                   TextWrapping="Wrap"
                   Foreground="#555"
                   Text="Athena uses your own OpenAI API key for voice and text mode. The key is stored locally in Windows Credential Manager." />

        <PasswordBox x:Name="ApiKeyBox"
                     Grid.Row="2"
                     Height="32"
                     VerticalContentAlignment="Center"
                     PasswordChanged="OnPasswordChanged" />

        <TextBlock x:Name="ValidationText"
                   Grid.Row="3"
                   Margin="0,8,0,0"
                   Foreground="#9a3412"
                   TextWrapping="Wrap" />

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Width="86"
                    Margin="0,0,8,0"
                    IsCancel="True" />
            <Button x:Name="SaveButton"
                    Content="Save"
                    Width="86"
                    IsDefault="True"
                    Click="OnSave"
                    IsEnabled="False" />
        </StackPanel>
    </Grid>
</Window>
